# ═══════════════════════════════════════════════════════════════
#  Animus Frontend — Environment Variables
# ═══════════════════════════════════════════════════════════════
#
#  Copy to .env.local for development:  cp .env.example .env.local
#  For Railway: set these in the frontend service's Variables tab.
#
#  IMPORTANT: NEXT_PUBLIC_* variables are baked into the bundle
#  at BUILD TIME. Changing them requires a rebuild.
# ═══════════════════════════════════════════════════════════════

# ── Required ──────────────────────────────────────────────────

# Backend API URL (the server/ service)
#   Development:  http://localhost:4000
#   Railway:      https://<your-server-service>.railway.app
NEXT_PUBLIC_API_URL=http://localhost:4000

# ── Optional ──────────────────────────────────────────────────

# Solana network (mainnet-beta | devnet | testnet)
NEXT_PUBLIC_SOLANA_NETWORK=mainnet-beta

# Sentry DSN for error monitoring (leave empty to disable)
#   Get from: https://sentry.io → Project Settings → Client Keys (DSN)
NEXT_PUBLIC_SENTRY_DSN=

# Sentry source map upload (CI only — not needed locally)
# SENTRY_AUTH_TOKEN=
# SENTRY_ORG=
# SENTRY_PROJECT=
