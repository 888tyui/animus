# ═══════════════════════════════════════════════════════════════
#  Animus Server — Environment Variables
# ═══════════════════════════════════════════════════════════════
#
#  Copy to .env for development:  cp .env.example .env
#  For Railway: set these in the server service's Variables tab.
#
#  All variables marked [REQUIRED] must be set or the server
#  will refuse to start.
# ═══════════════════════════════════════════════════════════════

# ── Required ──────────────────────────────────────────────────

# [REQUIRED] PostgreSQL connection string
#   Development:  postgresql://user:password@localhost:5432/animus
#   Railway:      auto-injected when you add a PostgreSQL plugin
DATABASE_URL=postgresql://user:password@localhost:5432/animus

# [REQUIRED] JWT signing secret — minimum 32 characters
#   Generate with:  openssl rand -hex 32
JWT_SECRET=

# [REQUIRED] GitHub Personal Access Token (raises rate limit from 60 to 5000 req/hr)
#   Generate at:  https://github.com/settings/tokens
#   Scopes needed: (none — public repo access only)
GITHUB_TOKEN=

# [REQUIRED] CORS allowed origin — your frontend URL
#   Development:  http://localhost:3000
#   Railway:      https://<your-frontend-service>.railway.app
CORS_ORIGIN=http://localhost:3000

# ── Optional ──────────────────────────────────────────────────

# Server port (Railway auto-assigns via PORT env)
PORT=4000

# Solana RPC endpoint
#   Default (free, rate-limited):  https://api.mainnet-beta.solana.com
#   Recommended for production:    Helius / QuickNode / Alchemy
SOLANA_RPC_URL=https://api.mainnet-beta.solana.com

# Sentry DSN for error monitoring (leave empty to disable)
#   Get from: https://sentry.io → Project Settings → Client Keys (DSN)
SENTRY_DSN=
